version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - npm install -g pnpm
        - pnpm install
    build:
      commands:
        - cd frontend
        - pnpm run build
  artifacts:
    baseDirectory: frontend/dist
    files:
      - '**/*'